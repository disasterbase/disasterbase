create table organization
(
    organization_id unsigned int,
    name text,
    type text,
    note text,
    size text,
    description text,
    feed_url text,
    qr_url text,
    url text,
);

create table operation
(
);
