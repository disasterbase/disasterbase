<%perl>
$m->redirect('/') unless Disaster::Authentication::i_am_god(); 
</%perl>
<h1>Users</h1>
<pre>
	<% Data::Dumper::Dumper( Disaster::SQLEngine::sqldb->fetch_select(
			table => 'user', order => 'approved'
		)) %>
</pre>

<h1>Organizations</h1>
<pre>
	<% Data::Dumper::Dumper( Disaster::SQLEngine::sqldb->fetch_select(
			table => 'organization', order => 'approved'
		)) %>
</pre>

<h1>Disasters</h1>
<pre>
	<% Data::Dumper::Dumper( Disaster::SQLEngine::sqldb->fetch_select(
			table => 'disaster', order => 'approved'
		)) %>
</pre>

<h1>Projects</h1>
<pre>
	<% Data::Dumper::Dumper( Disaster::SQLEngine::sqldb->fetch_select(
			table => 'project', order => 'approved'
		)) %>
</pre>
